// tests/setup.ts
import '@testing-library/jest-dom/vitest';
import { expect } from 'vitest';

// Optional: You can add global setup/cleanup here if needed
// e.g., beforeAll(() => { ... });
// afterEach(() => { cleanup(); }); // cleanup is often automatically handled by @testing-library/react
